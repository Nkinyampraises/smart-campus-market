apiVersion: apps/v1
kind: Deployment
metadata:
  name: listing-service
  namespace: campustrade
spec:
  replicas: 2
  selector:
    matchLabels:
      app: listing-service
  template:
    metadata:
      labels:
        app: listing-service
    spec:
      containers:
        - name: listing-service
          image: praisesn/listing-service:latest
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: campustrade-config
          env:
            - name: PORT
              value: "3003"
            - name: SERVICE_NAME
              value: "listing-service"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: campustrade-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: campustrade-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: campustrade-secrets
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: listing-service
  namespace: campustrade
spec:
  selector:
    app: listing-service
  ports:
    - port: 3003
      targetPort: 3003
  type: ClusterIP
