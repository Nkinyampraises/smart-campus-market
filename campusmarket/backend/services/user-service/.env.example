PORT=3002
SERVICE_NAME=user-service

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=campustrade
DB_PASSWORD=your_password
DB_NAME=campustrade_users

# JWT (same secret across all services)
JWT_SECRET=your_jwt_secret_here

# Redis (event bus)
REDIS_HOST=localhost
REDIS_PORT=6379
