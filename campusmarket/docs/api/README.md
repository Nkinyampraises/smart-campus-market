# CampusTrade API documentation

The production API publishes an OpenAPI 3.1 contract at `/api/docs/openapi.yaml` and a human-readable index at `/api/docs`.

The contract covers the public operational endpoint and the primary authentication, marketplace, search, offer, conversation, notification, and AI-assisted pricing flows. Protected endpoints use bearer JWT authentication.
